{
  "schemaVersion": 1,
  "id": "stereojukebox",
  "version": "${version}",
  "name": "StereoJukebox",
  "description": "Makes music discs play in both ears instead of depending on the player's position.",
  "authors": [],
  "contact": {},
  "license": "GPL-3.0",
  "icon": "assets/stereojukebox/icon.png",
  "environment": "client",
  "entrypoints": {
    "client": [
      "net.zbound.stereojukebox.client.StereoJukeboxClient"
    ],
    "main": [
      "net.zbound.stereojukebox.StereoJukebox"
    ]
  },
  "mixins": [
    "stereojukebox.mixins.json",
    {
      "config": "stereojukebox.client.mixins.json",
      "environment": "client"
    }
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "${minecraft_version}"
  }
}
